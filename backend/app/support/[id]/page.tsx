'use client';
import {useEffect,useState} from 'react';
export default function Ticket({params}:{params:{id:string}}){const[t,setT]=useState<any>();const[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/support/'+params.id);const x=await r.json();if(x.ok)setT(x.ticket)}useEffect(()=>{load()},[]);async function send(){if(!msg.trim())return;const r=await fetch('/api/support/'+params.id,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content:msg})});if(r.ok){setMsg('');load()}}return <main dir="rtl" className="min-h-screen bg-slate-50 p-5"><div className="mx-auto max-w-2xl"><a href="/support" className="font-bold text-primary-900">← الدعم</a>{t&&<><div className="mt-5 rounded-2xl border bg-white p-5"><h1 className="text-xl font-black">{t.subject}</h1><p className="text-sm text-slate-500">{t.ticketNumber} · {t.status}</p></div><div className="my-4 space-y-3">{t.messages.map((m:any)=><div key={m.id} className="rounded-2xl border bg-white p-4"><p className="whitespace-pre-wrap">{m.content}</p><small className="text-slate-400">{new Date(m.createdAt).toLocaleString('ar-YE')}</small></div>)}</div><div className="flex gap-2"><input value={msg} onChange={e=>setMsg(e.target.value)} placeholder="اكتب ردك..." className="flex-1 rounded-xl border p-3"/><button onClick={send} className="rounded-xl bg-primary-900 px-5 text-white">إرسال</button></div></>}</div></main>}
